package com.surveyplatform.model;

// Enum defining user roles for role-based access control
public enum Role {
    USER,   // Standard user who can create surveys and submit responses
    ADMIN   // Administrator with full access to manage all surveys and users
}
