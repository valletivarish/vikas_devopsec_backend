package com.surveyplatform.model;

// Enum defining survey visibility levels
public enum SurveyVisibility {
    PUBLIC,     // Visible to anyone with the link
    PRIVATE     // Only visible to the creator and invited participants
}
